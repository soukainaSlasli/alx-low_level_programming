#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the seconde number to multiply with first number
 * Return: the result of multiplication between x paran and y paran
 */

int mul(int x, int y)

{
	return (x * y);
}
